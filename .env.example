# Upstash Redis (Vercel Marketplace > Upstash > Redis). Both are injected by Vercel
# once the integration is linked; copy them here for `vercel dev`.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
